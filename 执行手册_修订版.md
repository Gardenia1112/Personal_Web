# 个人网站执行手册 · 修订版

> **适用**：`G:\PersonalWeb`（Astro 5 + TypeScript + Three.js + Phaser 4.2.1 + GSAP + Tailwind v4）
> **本版变更**：修正 2 项决策（转场统一 / 作品·奖项改横向视差画廊）+ 新增「附录 D 参考素材索引」+ 每 Phase 顶部加「📂 本阶段参考」引导 Cursor 读原文件
> **角色分工**：👤 你（手动敲命令/验收/提交）｜🤖 Claude Code（只规划不改码）｜🖥️ Cursor（按方案写码 + 跑 build）

---

## 铁律（沿用 AGENTS.md）

1. **Claude Code 只规划，Cursor 只实现** —— 不交叉
2. **Git 提交永远手动** —— AI 不碰 `git`
3. **每阶段必须通过验收才能进下一阶段**
4. **只做精修，不重构**（PROTECTED：已有架构经确认，别换栈）
5. **参考素材 Cursor 自己读原文件** —— 手册正文只放「路径索引」，不放截图/视频内容

---

# 第 0 部分 · 决策锁定表（v2 修订）

## 0.1 核心架构（转场统一，已确认）

```
主页 /  (3D 深夜工位，2.5D 等距)
  ├─ 点击【电脑】     → 镜头飞近 + 渐黑 → /desktop     (2D 桌面 OS)
  ├─ 点击【手柄】     → 镜头飞近 + 渐黑 → /about        (2D 游戏化)
  ├─ 点击【日记本】   → 镜头飞近 + 渐黑 → /contact      (2D 手写信)
  ├─ 点击【文件堆】   → 镜头飞近 + 渐黑 → /awards       (2D 横向视差画廊)
  ├─ 点击【台灯】     → 不跳转，切换昼夜（全局）
  └─ 点击【咖啡杯】   → 不跳转，奶茶彩蛋吐司

电脑桌面 /desktop  (2D 类 OS)
  ├─ 双击【红文件夹】 → 渐黑 → /works              (开发作品 · 横向视差画廊)
  ├─ 双击【黄文件夹】 → 渐黑 → /works?category=art  (美术作品 · 横向视差画廊)
  ├─ 双击【Logo】     → 渐黑 → /blog                (博客占位)
  └─ 双击【Resume】   → 渐黑 → /resume              (简历 PDF)
```

> ✅ **已确认**：电脑与其他物件**完全一致** = 镜头飞近 + 渐黑。**不存在**"平移进入桌面、停留在桌面内"的分支。所有跳转统一走 `TransitionController`（渐黑遮罩持久，跨页不闪烁）。

## 0.2 决策总表

| 编号 | 决策项 | 结论 |
|---|---|---|
| D1 | 主页 | 3D（2.5D 等距，Three.js） |
| D2 | 分页面 | 纯 2D |
| D3 | 电脑桌面 | 纯 2D（图标网格 + 窗口系统） |
| D4 | glb 组织 | 单文件 `room_full.glb`（点击靠 mesh 命名 `obj_*`） |
| **D5** | **物件点击转场** | **✅ 全部统一：相机飞近 + 渐黑（含电脑）** |
| D6 | 桌面图标点击转场 | 渐黑（复用 TransitionController） |
| D7 | 昼夜切换 | 全局（主页 + 桌面 + 2D 页，localStorage） |
| D8 | 触发昼夜物件 | 台灯 |
| D9 | 咖啡杯 | 奶茶彩蛋吐司 |
| **D10** | **作品页 / 奖项页布局** | **🔴 修订：横向视差画廊（codrops HorizontalParallaxGallery 风格），非普通网格** |
| D11 | 奖项入口 | 文件堆 → /awards |
| D12 | 光标 | heron 混合式（自定义 div + CSS cursor 兜底） |
| D13 | 小人 | Phaser 2D 精灵（仅 /about），v4.2.1 已实测兼容 |
| D14 | 响应式 | 移动端降级静态截图 + CSS 平移 |
| D15 | 桌面图标数 | 4 个（红/黄/Logo/Resume） |
| D16 | Resume | 独立路由 `/resume`，渲染 PDF |
| D17 | 路由参数 | 美术作品 `?category=art` |
| D18 | 返回方式 | 无返回按钮，顶部标签页导航闭环 |
| D19 | 编辑器分工 | Claude Code 规划 + Cursor 实现 |
| D20 | 提交方式 | 手动 `git commit`（每阶段一次） |

## 0.3 技术栈

```
框架：     Astro 5.x + View Transitions
3D：      Three.js (GLTFLoader, Raycaster, GSAP)
2D 趣味： Phaser 4.2.1（仅 /about）
样式：    Tailwind v4 + CSS3（@theme token 集中配色）
内容：    Astro Content Collections（博客）
动效：    GSAP + 原生 lerp/rAF（横向画廊走 lerp，见 D10）
部署：    Cloudflare Pages
```

> ⚠️ **禁止**：React / R3F / SCSS / typed.js。作品/奖项画廊用 **Astro + Three.js + lerp** 实现（融合进现有栈），**不要**另起 Vite 项目。

---

# 第 1 部分 · 开始前（核对，不重建）

## 1.1 环境验证 👤 手动

```powershell
cd G:\PersonalWeb
node -v          # >= 18
npm -v
Get-Content package.json | Select-String "astro"
Get-ChildItem public\models\ -Recurse -Include *.glb          # room_full.glb 在位
Get-ChildItem handoff\references\ -Recurse | Select-Object FullName, Length  # 参考素材在位
```

## 1.2 .gitignore 验证 👤 手动

```powershell
git check-ignore -v public/models/room_full.glb
git check-ignore -v handoff/references/recordings/*.mp4
# 有输出 = 安全；无输出 = 规则未生效，立刻修
```

## 1.3 前置对齐（每次新开 Cursor 对话必做） 🖥️ Cursor

> 把以下内容作为**第一条消息**发给 Cursor，让它先读文件再动手，防止基于旧文档返工。

```
请先按顺序阅读以下文件（只读不改），然后输出一份「当前现状摘要」：
1. handoff/PROJECT_STATE.md
2. handoff/AGENTS.md
3. docs/01-内容结构文档.md
4. docs/02-视觉交互设计系统.md
5. docs/03-技术架构文档.md
6. handoff/tasks/ 下我指定的那张任务卡
7. 本阶段「附录 D」中列出的参考文件（路径相对于项目根）

读完输出：
- 你现在负责哪个 Phase、入场状态是什么
- 已锁定的硬约束（来自 0.2 决策表）有哪些
- 你打算改哪些文件、新建哪些文件（先列清单，等我确认再动手）

⚠️ 绝对规则（来自 AGENTS.md §5）：
- 不改 docs/01、docs/02 正文
- 英文命名、资源走 public/assets/
- 内容唯一来源 src/data/*.ts，禁止硬编码进 .astro
- 禁止 TODO 空壳，不确定标 ⚠️ 待确认
- 不要自动 commit / push
```

**你必须做的**：Cursor 输出 mini-plan + 文件清单后，**你确认无误**才让它动手。这一步别省——防它一把梭改 20 个文件。

---

# 第 2 部分 · 资产准备（核对命名规范）

## 2.1 Blender mesh 命名规范 👤 手动

`room_full.glb` 每个可点物件根 mesh 必须按此命名：

| mesh 名 | 物件 | 点击后 |
|---|---|---|
| `obj_computer` | 电脑 | → /desktop |
| `obj_gamepad` | 手柄 | → /about |
| `obj_notebook` | 日记本 | → /contact |
| `obj_filepile` | 文件堆 | → /awards |
| `obj_lamp` | 台灯 | 昼夜切换 |
| `obj_coffee` | 咖啡杯 | 奶茶彩蛋 |
| `obj_room` | 房间壳 | 不可点 |

规则：前缀 `obj_`、全小写+下划线、独立 mesh、原点在底部中心、单位=米。

## 2.2 命名校验脚本 🖥️ Cursor

```typescript
// scripts/check-glb-names.ts
import { GLTFLoader } from 'three-stdlib';
const REQUIRED = ['obj_computer','obj_gamepad','obj_notebook','obj_filepile','obj_lamp','obj_coffee','obj_room'];
const loader = new GLTFLoader();
loader.load('/models/room_full.glb', (gltf) => {
  const names = gltf.scene.children.map(c => c.name);
  const missing = REQUIRED.filter(r => !names.includes(r));
  if (missing.length) { console.error('❌ 缺失 mesh:', missing); process.exit(1); }
  console.log('✅ 所有必需 mesh 命名正确');
});
```

## 2.3 Draco 导出 👤 手动
Blender 导出时勾选：Draco compression + Embed Textures + Y 向上。

---

# 第 3 部分 · 主页 3D（精修 · 对应 PHASE1-2 卡）

> **入场状态**：✅ 已完成（代码 `src/components/Desk3D/Scene.astro` + `src/scripts/desk.ts`，数据 `src/data/objects.ts`）。本阶段为**精修**。

## 📂 本阶段参考（Cursor 执行前先读）

- `handoff/references/recordings/john-particias-comfort-space-motion.mp4` + `.md`
  → **鼠标移动带动视角摇晃（parallax 视角跟随）** + **白天黑夜切换**，必须复刻
- `handoff/references/awwwards/john-particias-comfort-space.md` → 同上说明
- `handoff/references/awwwards/style-ref-01.png` / `style-ref-02.png` → 整体配色与 3D 氛围参考

## 3.1 精修项 🖥️ Cursor

```
在现有 src/scripts/desk.ts 基础上精修（不要重写架构）：

【精修 1：鼠标视角摇晃】
- 监听 mousemove，归一化到 [-1, 1]
- 用 lerp 平滑插值 camera.position / OrbitControls target（或手动控制相机）
- 摇晃幅度要克制（参考录屏），不眩晕
- 仅在桌面端生效（pointer: fine），移动端跳过

【精修 2：昼夜切换强化】
- 现有 lszbf:theme 已切换光照，核对：
  - 白天：HemisphereLight + DirectionalLight，暖色调
  - 黑夜：PointLight（台灯暖光）+ 弱 Ambient，冷色调
- 状态存 localStorage，跨页读取（桌面也跟着变）
- 核对两套管线都生效（无光照突变）

【精修 3：状态机手感】
- IDLE → HOVER → ACTIVE 三态 GSAP timeline 缓动时长/幅度
- hover 渐入、click 激活、移出回落顺滑无抖动

【精修 4：开场剧本】
- 核对 ①-⑧ 步时序与 localStorage["lszbf:intro:played"]
- 二次访问确实跳过

【精修 5：占位几何体】
- 6 物件按各自 geometry 区分形状，非清一色立方体

【精修 6：移动端降级】
- matchMedia("(pointer: coarse)") / clientWidth < 768 → 静态 DOM 菜单，不加载 3D，不白屏
- 真机 + DevTools 两种都要过

转场统一走 TransitionController（渐黑），勿在此引入新的转场方式。
完成后 npm run build 确认无报错，git status 汇报（不 commit）。
```

## 3.2 验收 👤 手动

```
□ npm run build 无报错
□ 首页 3D 工位渲染正常，6 物件可点击并跳转正确路由（统一渐黑）
□ 🔴 鼠标移动 → 视角跟随摇晃（克制，不眩晕）
□ 🔴 台灯切换昼夜 → 光照正确（白天暖/黑夜台灯暖光），跨页保持
□ 开场剧本首次播放、二次访问跳过
□ hover 三态顺滑无抖动
□ 移动端降级为静态菜单，无 3D、无白屏
□ 离开主页 3D 销毁，无内存泄漏（DevTools Memory）
```

## 3.3 提交 👤 手动

```powershell
git add -A; git status   # 确认无 .glb/.mp4
git commit -m "refactor(phase1-2): 鼠标视角摇晃 + 昼夜强化 + 状态机精修"
```

---

# 第 4 部分 · 电脑桌面（精修 · 对应 PHASE4 卡）

> **入场状态**：✅ 已完成（`src/pages/desktop.astro` + `src/scripts/desktop/desktop.ts`）。**精修**。

## 📂 本阶段参考

- `handoff/references/recordings/graffico-office-desktop-motion.mp4` + `.md`
  → **点击物件后进入桌面 OS、文件夹可互动**的整体流程与节奏
- `handoff/references/awwwards/style-ref-01.png` / `style-ref-02.png` → 桌面 3D 展示 + 配色
- `handoff/references/awwwards/graffico-office.md` → 同上

## 4.1 精修项 🖥️ Cursor

```
在现有桌面 OS 上精修（保持原生 JS，别引入 React）：

【精修 1：窗口系统】
- 开窗 / 拖拽 / 关闭 / 聚焦全流程顺滑
- 多窗 z-index 管理正确，无卡死或残留

【精修 2：图标 → 应用映射（4 图标，已定）】
| 图标       | 路由                        |
| 红色文件夹 | /works（开发项目）          |
| 黄色文件夹 | /works?category=art（美术） |
| Logo 贴纸  | /blog                       |
| 简历文档   | /resume.pdf                 |

【精修 3：视觉对齐录屏】
- 对照 graffico-office-desktop-motion.mp4，核对：
  - 进入桌面时的展开节奏/动效
  - 文件夹点击反馈
  - 整体拟物质感
- 窗口边框/阴影/配色走 global.css @theme token，不写死色值

【精修 4：Resume】
- /resume.pdf 可访问（desktop.ts 与 contact.astro 两处统一）
- 确认 public/resume.pdf 在位

【精修 5：移动端】
- 触屏可用（拖拽降级为点按，或提示用桌面端），不白屏

【精修 6：位置持久化】
- 图标位置存 localStorage，刷新保持
```

## 4.2 验收 👤 手动

```
□ /desktop 正常打开，窗口可开/拖/关，无 JS 报错
□ 双击红文件夹 → 渐黑 → /works（横向画廊）
□ 双击黄文件夹 → 渐黑 → /works?category=art（横向画廊）
□ 双击 Logo → 渐黑 → /blog
□ 双击 Resume → 渐黑 → /resume（PDF 显示）
□ 顶部导航点击也走渐黑
□ 视觉/节奏对齐 graffico 录屏
□ 移动端触屏可用
```

## 4.3 提交 👤 手动

```powershell
git commit -m "refactor(phase4): 桌面 OS 窗口交互 + 视觉对齐 graffico 参考"
```

---

# 第 5 部分 · 关于页（精修 · 对应 PHASE3 卡）

> ✅ 已完成（Phaser v4.2.1 已实测兼容，代码零改动）。**最低优先，仅核对**。

## 📂 本阶段参考

- `handoff/references/recordings/heron-ai-cursor-and-page-transition.mp4` + `.md`
  → **小人移动时的页面切换动效**（如可用到 /about 入场）

## 5.1 核对项 🖥️ Cursor

```
核对 src/scripts/game/ 下四输入手感、4 展板触发、ESC 返回、Phaser 分包隔离。
入场切换动效若有提升空间，参考 heron 录屏的小人移动转场。
不重构，只做手感精修。
```

## 5.2 提交 👤 手动

```powershell
git commit -m "refactor(phase3): 关于页输入手感 + 入场动效精修"
```

---

# 第 6 部分 · 🔴 作品页 + 奖项页改造（横向视差画廊）

> **这是本修订版的核心变更**：D10 从"普通网格"改为"**横向视差画廊**"（codrops HorizontalParallaxGallery 风格）。
> **现状**：Phase 5 原实现是普通网格。本阶段要**改造为横向视差**，但保持路由与数据层不变（`/works`、`/works?category=art`、`/awards`）。

## 📂 本阶段参考（全部） ⚠️ Cursor 必读

- `handoff/references/recordings/portfolio-showcase-motion.mp4` + `.md`
  → **横向视差画廊本体效果**（作品 + 奖项都用此风格）
- `handoff/references/codrops/project-card-to-detail.md`
  → **卡片点击 → 详情页跳转**的 Codepen 参考
- `handoff/references/awwwards/christoph-nagel-project-detail-1.png` / `-2.png` + `.md`
  → **详情页排版风格**（点击画廊卡片后进入的详情页）
- `handoff/references/codrops/motion-links.md` → 动效链接索引

### 参考实现要点（已核对 codrops 原教程，Cursor 据此实现）

```
技术栈：Astro + Three.js + 原生 lerp/rAF（融合进现有栈，勿另起 Vite）

【布局】横向排列
- 容器：display: flex; gap: 2rem; width: max-content; will-change: transform
- 每项：.gallery__media { overflow: hidden; height: 60vh; aspect-ratio: 4/3; flex-shrink: 0 }
- 图片：width: 125%; left: -12.5%（预留视差空间）

【滚动】平滑横向滚动
- 自定义 scroll（横向），用 lerp 插值：
  scroll.current = lerp(scroll.current, scroll.target, ease)
- rAF 循环：container.style.transform = translateX(${-scroll.current}px)
- ⚠️ 禁用 GSAP 做滚动本体（原教程即 lerp+rAF），GSAP 仅用于入场/点击动效

【视差】图片在容器内横向位移
- 方案 A（DOM）：图片元素随 scroll 做额外 translateX（0.2~0.3 倍差）
- 方案 B（WebGL）：Three.js PlaneGeometry + ShaderMaterial，coverUv + uParallax（进阶，可选）
- 初版走方案 A，跑通后再考虑 B

【数据】复用现有 src/data/
- /works：projects.ts（dev）→ 无参数
- /works?category=art：projects.ts（art）→ ⚠️ 读取方式锁定：
  ❌ 禁止 Astro.url.searchParams（静态站构建期无 query，永远返回 dev）
  ✅ 用客户端 JS 读 location.search 切换 tab（默认 dev，?category=art 显示美术）
- /awards：awards.ts

【分类切换】在画廊顶部保留 tab：全部 / 开发 / 美术（仅 /works 页需要）
- 切 tab 时：用 GSAP 做画廊重新排布的过渡动画

【点击 → 详情】走 View Transitions
- 卡片点击 → 渐黑（复用 TransitionController）→ /works/[slug]
- 详情页排版复刻 christoph-nagel 风格（大图 + 排版留白 + 四段式）
```

## 6.1 执行步骤 🖥️ Cursor

```
Phase 6A：横向画廊组件（works + awards 共用）
1. 新建 src/components/HorizontalGallery.astro + src/scripts/gallery.ts
2. 实现横向 flex 布局 + lerp 平滑滚动 + 图片视差（方案 A）
3. 接收 props：items（封面/标题/标签/slug 数组）
4. /works、/awards 两页各自调用该组件（数据来源不同）
5. 移动端：横滑降级为 touch 横滑（保留横向，不用竖直列表）

Phase 6B：分类 tab（仅 /works）
1. 画廊顶部 tab：全部 / 开发 / 美术
2. 客户端读 location.search（⚠️ 不用 Astro.url.searchParams）
3. 切 tab 用 GSAP 过渡

Phase 6C：详情页排版
1. 更新 src/pages/works/[slug].astro
2. 排版复刻 christoph-nagel 风格：大封面 + 标题 + 四段式（problem/tech/contribution/result）
3. 数据来自 Content Collections 或 projects.ts（按现有约定）

Phase 6D：奖项页改用同一画廊
1. src/pages/awards.astro 从"普通网格"改为复用 HorizontalGallery
2. 数据 awards.ts，分组 全部/竞赛获奖/证书（校级院级默认隐藏开关保留）
```

## 6.2 验收 👤 手动

```
□ /works 横向视差画廊渲染，滚轮/拖拽横向移动顺滑
□ 图片视差效果可见（非静止）
□ 切 tab（全部/开发/美术）正常，location.search 生效
  ⚠️ 必须在 npm run preview（模拟静态站）下测试，不能只在 dev 测
  （dev 有 query 会掩盖 bug）
□ /works?category=art 直接访问显示美术作品
□ 卡片点击 → 渐黑 → 详情页，排版对齐 christoph-nagel
□ /awards 同样横向画廊风格
□ 移动端横滑可用
□ npm run build 通过
```

## 6.3 提交 👤 手动

```powershell
git commit -m "feat(phase6): 作品/奖项改横向视差画廊（codrops 风格）+ 详情页排版"
```

---

# 第 7 部分 · 全局界面（精修 · 对应 PHASE6 卡剩余项）

## 📂 本阶段参考

- `handoff/references/recordings/heron-ai-cursor-and-page-transition.mp4` + `.md`
  → **自定义光标 + 页面切换动效**
- `handoff/references/awwwards/heron-ai.md` → 简约风格 + 光标

## 7.1 光标 🖥️ Cursor

```
实现 heron 混合式光标（参考录屏）：
- 固定 div 跟随鼠标（transform: translate，lerp 平滑）
- 可点击区域（物件/链接）光标放大/变图标
- CSS cursor 作兜底（自定义 div 隐藏时）
- 文件：src/scripts/cursor.ts + src/styles/cursor.css
```

## 7.2 昼夜全局化 🖥️ Cursor

```
- store: src/store/daynight.ts（localStorage）
- 主页：改 Three.js 光照（Phase 3 已做，核对）
- 2D 页：<body data-theme="day|night"> + CSS 变量切配色
- 初始读 localStorage，无则跟系统 prefers-color-scheme
- 🔴 两套主题下把全部页面扫一遍（含画廊页）
```

## 7.3 彩蛋 + 性能 + 响应式（原 PHASE6 卡精修项） 🖥️ Cursor

```
- 咖啡/耳机/贴纸彩蛋核对（coffee→邮箱/微信、headphones、sticker）
- 移动端降级验证（matchMedia + ?three=off 调试开关）
- Lighthouse Performance 目标 > 85
- 全站色值引用 @theme token，排查硬编码
- 断链排查：/assets/、/resume.pdf、奖项图（awards.ts 的 file 字段）
- 可访问性：hover 有键盘/焦点替代、aria-live
```

## 7.4 验收 👤 手动

```
□ 自定义光标在桌面/主页/画廊页生效
□ 主页点台灯 → 进 /works 仍是对应主题配色
□ 彩蛋可触发、文案正确
□ 移动端降级为静态菜单，无 3D、无白屏
□ Lighthouse Performance > 85
□ 无 404 断链/断图
□ 主题切换在两套管线下全页通过
```

## 7.5 提交 👤 手动

```powershell
git commit -m "refactor(phase6): heron光标 + 昼夜全局化 + 彩蛋 + 性能 + 响应式"
```

---

# 第 8 部分 · 博客 + 最终 QA（Phase 7 · Part A 先做）

## 8.1 博客 /blog 🖥️ Cursor

```
- Astro Content Collections：src/content/blog/*.md + src/content/config.ts
- 列表页 /blog + 详情 /blog/[slug]
- 首批 3 篇（占位文案，你后续替换）：
  1.《用 AI 工具链搭游戏客户端作品集》
  2.《流浪尸潮 35→60FPS 优化实录》
  3.《Unity 泛型状态机在肉鸽中的应用》
```

## 8.2 联系页 /contact 🖥️ Cursor

```
- 手写信风格表单（姓名/邮箱/留言）
- 提交用 Formspree 或 Cloudflare Workers（占位 key）
- 打字机入场动画
```

## 8.3 最终 QA 清单 👤 手动

```
【转场】所有跳转走渐黑（主页物件/桌面图标/顶部导航），遮罩跨页持久不闪烁
【3D】工位渲染正常、mesh 命名正确、离页销毁无泄漏、移动端降级
【画廊】/works + /works?category=art + /awards 横向视差可用，tab 切换正常，详情页排版对齐 christoph-nagel
【2D】/desktop 4 图标、/about Phaser、/blog、/contact 正常
【全局】昼夜全局、heron 光标、顶部导航闭环、响应式（375/768/1440）
【资产】glb/mp4 未入库（git check-ignore 通过）、Draco 生效、无 404
```

## 8.4 提交 👤 手动

```powershell
git commit -m "feat(phase7): 博客占位 + 联系页 + 横向画廊 + 最终 QA 通过"
git tag -a v1.0 -m "首个完整版本"
```

---

# 第 9 部分 · 部署（Phase 7 · Part B · 最后做）

> ⚠️ **部署放最后**：先打磨完（Phase 1-6 精修 + 画廊改造）再上线。

## 9.1 Cloudflare Pages 👤 手动

```
1. 推送代码到 GitHub 公开仓库
2. Cloudflare Dashboard → Pages → 连接 GitHub
3. 构建设置：Framework preset: Astro / Build: npm run build / Output: dist / NODE_VERSION=20
4. 获得 *.pages.dev 域名
5. 自定义域名 lszbf.com + www → DNS/NS 指向 Cloudflare → 自动 HTTPS
```

> ⚠️ glb/mp4 不入库：靠 .gitignore 排除，确保 public/ 下文件被 Astro 复制到 dist/。备选：大文件放 R2/CDN 引用 URL。

## 9.2 上线验证 👤 手动

```
□ 主页 3D 加载正常（线上）□ 所有转场流畅 □ 画廊横向视差可用
□ 昼夜/光标/响应式正常 □ Lighthouse 性能 > 85（移动端）
```

---

# 附录 A · 命令速查

```powershell
npm run dev          # 开发
npm run build        # 构建（每阶段必跑）
npm run preview      # ⚠️ 模拟静态站，测 ?category=art 必须用它
npx tsx scripts/check-glb-names.ts
git check-ignore -v public/models/*.glb handoff/references/recordings/*.mp4

# 每阶段提交模板
git add -A
git status          # 务必确认无 .glb/.mp4
git commit -m "refactor(phaseX): ..."
```

# 附录 B · 故障排查

| 问题 | 原因 | 解决 |
|---|---|---|
| 点击物件无反应 | mesh 命名不对 | 跑 check-glb-names.ts |
| 渐黑后不跳转 | 遮罩未挂载 | 检查 #transition-overlay 在 `<body>` + View Transitions persist |
| 切页 3D 仍跑 | dispose 未执行 | 检查 astro:before-swap 监听 |
| glb 404 | 路径错 | 确认在 public/models/ 下 |
| 移动端卡顿 | 未降级 | matchMedia + ?three=off |
| **?category=art 不生效** | **用了 Astro.url.searchParams** | **改客户端 location.search** |
| 画廊不横向 | 走了竖直布局 | 核对 flex + width:max-content + translateX |
| 视差抖动 | lerp ease 过大/没 will-change | ease ~0.08、加 will-change: transform |
| 主题切换漏页 | store 未跨页读 | localStorage + 每页初始化 |

# 附录 C · 文件地图（现状 + 本版新增）

```
public/
  models/room_full.glb            # 3D 房间（不入库）
  assets/{brand,projects,awards}/ # 站点资源
  resume.pdf                     # 简历
handoff/
  references/                    # ⭐ 参考素材（三目录，见附录 D）
  tasks/                         # 6 张任务卡
  AGENTS.md / PROJECT_STATE.md
docs/                            # 01/02/03 权威规范（01/02 勿改）
src/
  components/
    Desk3D/Scene.astro           # 主页 3D
    HorizontalGallery.astro      # 🆕 横向画廊（works/awards 共用）
    TransitionOverlay.astro      # 渐黑遮罩
    TopNav.astro / DesktopIcon.astro
  scripts/
    desk.ts                      # 主页交互（状态机 + 视角摇晃 + 昼夜）
    desktop/desktop.ts           # 桌面 OS
    gallery.ts                   # 🆕 横向画廊逻辑（lerp + rAF）
    cursor.ts                    # heron 光标
    topnav.ts / main.ts
  store/daynight.ts              # 昼夜全局状态
  data/                          # 内容唯一来源（projects/awards/profile/objects/aboutGame）
  pages/
    desktop.astro / about.astro / contact.astro / resume.astro
    works/index.astro            # 🔄 改画廊
    works/[slug].astro           # 🔄 christoph-nagel 排版
    awards.astro                 # 🔄 改画廊
    blog/index.astro / blog/[slug].astro
  content/
    works/dev/*.md / works/art/*.md / awards/*.md / blog/*.md
```

# 附录 D · 参考素材索引 ⭐（本版核心新增）

> 所有路径相对于项目根 `G:\PersonalWeb\`。**Cursor 在每个 Phase 执行前按「📂 本阶段参考」读取对应文件**。手册正文只放路径 + 一句话用途，原始截图/视频 Cursor 自己读。

## D.1 awwwards\（视觉风格/排版参考，8 文件）

| 文件 | 一句话用途 | 落到的 Phase |
|---|---|---|
| `john-particias-comfort-space.md` | 3D 风格 + 鼠标摇视角 + 昼夜 | Phase 1-2 |
| `style-ref-01.png` / `style-ref-02.png` | 整体配色 + 3D 氛围 | Phase 1-2 / 4 |
| `graffico-office.md` | 桌面 3D 展示 + 配色 | Phase 4 |
| `heron-ai.md` | 简约风格 + 光标设计 | Phase 6（光标） |
| `christoph-nagel-project-detail-1.png` / `-2.png` | **详情页排版** | Phase 6（详情页） |
| `christoph-nagel-project-detail.md` | 详情页说明 | Phase 6 |

## D.2 codrops\（动效参考，2 文件）

| 文件 | 一句话用途 | 落到的 Phase |
|---|---|---|
| `motion-links.md` | 动效链接索引（codrops/codepen） | 全局 |
| `project-card-to-detail.md` | 卡片 → 详情跳转（Codepen） | Phase 6（点击→详情） |

## D.3 recordings\（动效录屏，6 文件，每个配同名 .md）

| 文件 | 一句话用途 | 落到的 Phase |
|---|---|---|
| `john-particias-comfort-space-motion.mp4` (42MB) | **3D 视角摇晃 + 昼夜切换**，必须复刻 | Phase 1-2 |
| `graffico-office-desktop-motion.mp4` (26MB) | **桌面 OS 交互流程 + 文件夹互动** | Phase 4 |
| `heron-ai-cursor-and-page-transition.mp4` (69MB) | **自定义光标 + 小人移动切换动效** | Phase 6（光标/转场） |
| `portfolio-showcase-motion.mp4` (21MB) | **横向视差画廊本体**，作品+奖项都用 | Phase 6（画廊） |

> 💡 Cursor 读取规则（写进提示词）：录屏用 `.md` 同名说明快速定位要点；**真正复刻效果时必须打开 .mp4 逐帧看**。

---

## ✅ 一句话总结

**Phase 1-6 代码主体已完成并构建通过，你不需要重建**。这一版手册只做三件事：
1. **修正 D5**（转场全部统一渐黑——你已确认电脑也一样）+ **D10**（作品/奖项从网格改为**横向视差画廊**）
2. 把 19 个参考文件**精确索引到具体 Phase**（附录 D），Cursor 按索引自己读原文件
3. 每阶段给了可直接复制的 Cursor 提示词，含前置对齐 + 硬约束（⚠️ `location.search` 不用 `Astro.url.searchParams`）

**你现在去 Cursor**：先跑 §1.3 前置对齐 → 从 §3（Phase 1-2 精修）开始。建议**先改 Phase 6 画廊**（D10 是最大变更，越早验证越好），再回头精修其余。每完成一阶段把 Cursor 的「改动文件清单」发我，我帮你核对是否偏离决策。🚀
