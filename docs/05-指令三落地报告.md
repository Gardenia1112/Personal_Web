# 05 · 指令三落地报告

> 执行方：Claude ｜ 验收：用户（赵彼方）
> 日期：2026-09-08 ｜ 范围：指令三 8 步（雅化 / 建模 / cover poster / 校园实拍 / 资产入库）
> 状态：第 1–7 项已落地并核对，待第 8 步本地提交（不 push）。

---

## 一、五项裁定清单

| # | 裁定项 | 结论 | 状态 |
|---|---|---|---|
| 1 | 画作口语化文件名雅化（6 个） | 6 个已改，磁盘/`art.ts`/`docs/04` 三处一致 | ✅ |
| 2 | 建模 slot4 改用成品渲染 | 风车参考 → `TD1.png`（TD2-4 备选），参考图移出 `public/` | ✅ |
| 3 | 辽大IP 主版本 | 5 slot 保持现状，无需改 | ✅ 确认 |
| 4 | air-track cover 用视频 poster | 本机 ffmpeg 不可用 → cover 留空 + TODO，未强跑 | ⚠️ 待 ffmpeg |
| 5 | 校园实拍 10 张入 now-art | 复制入 `now-art-lnu-01..10.jpg`，缩略图化，接入 now 画廊 | ✅ |

---

## 二、雅化映射表（第 1 项）

| 原文件名 | 新文件名 |
|---|---|
| `我擦，血腥中二病。原.png` | `血腥中二.png` |
| `我曹这是。。？.png` | `迷惘时刻.png` |
| `哈哈小两口荒野求生来的.png` | `荒野求生.png` |
| `很好的小孩打光.png` | `孩童光影.png` |
| `我曹有鬼红绿色盲.png` | `红绿色盲.png` |
| `无限流游戏卧槽.png` | `无限流游戏.png` |

> 原则：去网络口癖/脏字，保留意象。`paint()` 的 `label` 由文件名自动派生，故 `art.ts` 无需另改文案。
> 仍偏口语、超出「6 个」范围未改：`喜欢太白的老师生日快乐（？`、`又是梦女ww浴袍`、`这个也好鬼ts`、`生贺ww相框版`、`怎么这么文艺3`、`我们今天结婚了~tiaose`（见 docs/04 §4.4）。

---

## 三、modeling 占用（第 2 项）

| 槽位 | 主图 | 备选 |
|---|---|---|
| BF 模型 | `bf.png` | `bf2-4.png` |
| cx 模型 | `cx.png` | `cx2-3.png` |
| tys 模型 | `tys.png` | — |
| TD 模型 | `TD1.png` | `TD2-4.png` |

> 原「风车参考」槽已改为 TD 成品渲染；`风车参考.jpg` 已移出 `public/`（保留在 `_unused_photos/Models/`，不入库不引用）。`正.png`/`负.png` 无合适槽位留作备选；`Makima chibi/texture/*`、`气垫导轨虚拟实验纹理/*` 为贴图，不当主图。

---

## 四、air-track cover poster（第 4 项）

- 目标路径：`/assets/projects/qidian/qidian-poster.jpg`（从 `作品演示视频.mp4` 第 1 秒抽帧）。
- 现状：本机 ffmpeg 不可用（bash 与 PowerShell 均无）→ 未生成，`projects.ts` air-track `media.cover` 留空，并保留 TODO 注释（抽帧后启用一行即可）。
- 关联：docs/04 §5「视频转码规范」已列 `qidian-poster` 与其余待转码项。

---

## 五、校园实拍 10 张清单（第 5 项）

| 编号 | 地标 | 源文件（`_unused_photos/辽大IP/`） |
|---|---|---|
| 01 | 崇山校区银杏路 | `1/1/辽宁大学秋日校园素材/崇山校区银杏路.jpg` |
| 02 | 崇山校区体育馆 | `1/1/…/崇山校区体育馆.jpg` |
| 03 | 崇山校区哲理楼 | `1/1/…/崇山校区哲理楼.jpg` |
| 04 | 崇山校区机关楼 | `1/1/…/崇山校区机关楼.jpg` |
| 05 | 崇山校区枫树 | `1/1/…/崇山校区枫树.jpg` |
| 06 | 蒲河校区晚霞 | `1/1/…/蒲河校区晚霞.jpg` |
| 07 | 蒲河校区 | `1/1/…/蒲河校区.jpg` |
| 08 | 图书馆 | `2/2/照片/图书馆.jpg` |
| 09 | 物理楼 | `2/2/照片/物理楼.jpg` |
| 10 | 冬（冬季校园） | `2/2/照片/冬.jpg` |

> 复制不移动（原图仍在 `_unused_photos/`）；落点 `public/assets/about/now-art-lnu-*.jpg`，已追加进 `aboutGame.ts` 的 `now` 展板 `reel`（8 → 18 张），未覆盖原有 8 张。均按文件名地标筛选、未目视画质，可在后台替换。

---

## 六、.gitignore 新增规则（第 6.1 项）

在既有 `*.mp4` 基础上新增：

```
*.webm
*.psd   *.tif   *.psb          # 源文件，只提交导出的 png/jpg
*.blend *.fbx *.obj *.mtl *.max *.dae *.skp   # 3D/工程源文件，只提交成品渲染
_unused_photos/                 # 备查归档（原始 dump + 参考图）
~$*                             # Word 临时锁文件兜底
```

> 效果：`*.png`/`*.jpg` 入库；视频/源文件/工程文件/归档不入库。

---

## 七、资产入库核对（第 6.3–6.4 项）

- **已暂存 111 个文件**：106 张 png/jpg + `.gitignore` + `docs/04` + 3 个 `src/data/*.ts`。
- **违禁扩展名 staged：0**（无 mp4/webm/psd/tif/blend/fbx/obj/mtl/max/dae/skp，无 `_unused_photos/`）。
- **未跟踪 5 个源文档**（正确排除，不入库）：`qidian/教学文档.pdf`、`qidian/答辩PPT.pdf`、`zhilian/01-作品开发文档.pdf`、`zhilian/源码.docx`、`zhilian/03-答辩PPT.pptx`。
- **缩略图化（第 6.2 项，仅 `public/` 不动 `src/`）**：65 张缩到 ≤1200px（PNG 无损、JPG 质量 82），如 `企划2太阳月亮电影.png` 32MB→1.5MB、`now-art-lnu-07.jpg` 21MB→248KB。源图保留在 `assets/images/`（gitignore）。

---

## 八、构建与扫描核对（第 7 项）

- **`npm run build` ✅**：15 页构建通过（`Complete!`，36.89s）。
- **`grep "待补"`**：命中均为既有占位机制（`awards.ts` 类型联合、`[slug].astro` 的「待补素材」降级文案、air-track TODO 注释），无新增异常。
- **`canonical/og:url`**：仍为占位 `https://[TARGET_DOMAIN]…`（共 11 处），待用户填真实域名。
- **悬空引用扫描**：89 个 `/assets/` 路径，缺失 26 个，全部为**已文档化占位**，无真实断链：
  - zhilian 占位 `zhilian-*.png` ×12（docs/04 §1「无对应图 → 保留占位」）
  - qidian 占位 `qidian-*.png` ×8（docs/04 §2）
  - air-track poster `qidian-poster.jpg` ×1（TODO，见上）
  - `icon-*.svg` ×4 —— **误报**，实为 `src/assets/icons/` 的 Vite 导入（`?url`），非 `public/` 引用，文件在位、构建已解析。

---

## 九、待用户操作（3 类）

1. **手动填 `[TARGET_DOMAIN]**：上线前把 11 处 `https://[TARGET_DOMAIN]` 替换为真实域名（见 §八）。
2. **手动视频转 webm**：本机 ffmpeg 不可用，zhilian/qidian/art-videos 等视频的 `.webm` + 同名 `.jpg` poster 需在装有 ffmpeg 的环境补转（docs/04 §5）。
3. **日后补占位图**：zhilian ×12、qidian ×8 占位图，以及 air-track `qidian-poster.jpg` 抽帧（可在视频转码时一并生成）。

---

## 十、已知限制

- ffmpeg 不可用 → air-track cover、各视频 `.webm`/poster 均未生成（TODO，未强跑）。
- 校园实拍 10 张按文件名筛选，未目视画质/构图，用户可在后台替换。
- 存量类型错误 7 个（`npx tsc --noEmit`）为历史遗留，`npm run build` 不做类型检查故不影响构建（docs/04 之外未改动）。
- 插画仍有 6 个偏口语文件名未改（超出本次「6 个」范围，日后定名）。
