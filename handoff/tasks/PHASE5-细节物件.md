# PHASE 5：细节物件（键盘技能矩阵 + 证书翻页墙 + 本子 Links + 台灯主题）

> 阶段标签：Phase 5（细节物件）
> ⚠️ 对齐手册 §四：本 Phase 覆盖四个二级物件交互；证书墙已基本完成，其余三项为精修/补全。

---

## 目标

完善「细节物件」交互：键盘技能矩阵、纸质文件夹证书墙、本子 Links、台灯主题切换。

---

## 对应文档

- `../docs/01-内容结构文档.md` §2（物件→路由）、§6（奖项内容）
- `../docs/02-视觉交互设计系统.md` §2（物件映射）、§3.3（证书翻页墙）
- `../docs/03-技术架构文档.md` §3（objects.ts 物件映射）、§5.3（awards.ts 字段）、§5.4（objects.ts）

---

## 入场状态

部分完成：

- ✅ **证书墙**（folder → `/awards`）：`src/pages/awards.astro` + `src/data/awards.ts` 已完成（奖学金已删，分组 全部/竞赛获奖/证书/待补）。
- ✅ **本子 Links**（notebook → `/contact`）：`src/pages/contact.astro` 已存在。
- ✅ **台灯主题**（lamp → 主题切换）：`localStorage["yb-design:theme"]` 已实现。
- ⚠️ **键盘技能矩阵**（keyboard → skills）：`objects.ts` 已映射，实现状态需在 `desk.ts` 核对。

本卡是**精修**。

---

## 执行步骤（精修项）

1. **证书墙素材补位**：核对 `awards.ts` 里 `file` 占位项，把实际扫描件放进 `public/assets/awards/`（英文命名）。
2. **校级/院级隐藏开关**：`Award.hide?` 控制「校级院级」默认隐藏，确认生效。
3. **键盘技能矩阵**：确认 keyboard 物件触发 skills 矩阵，数据来自 `profile.ts` 的 `skills`。
4. **本子 Links**：核对 `/contact` 联系方式 + 外链（github/gitee/邮箱/微信）与 `profile.ts` 一致。
5. **台灯主题切换**：确认 `yb-design:theme` 深/浅两套样式都生效。
6. **数模省一占位**：全国大学生数学建模竞赛辽宁省一等奖 = 占位，标 `⚠️ 待确认`。

---

## 验收标准

- [ ] 证书墙分组正确、图片可预览、无断图。
- [ ] 校级/院级隐藏开关生效。
- [ ] 键盘技能矩阵、本子 Links、台灯主题三处交互正常。
- [ ] `npm run build` 通过。

---

## 风险与提醒

- ⚠️ **证书扫描件待用户提供**：`file` 占位项标 `⚠️ 待确认`，别编造路径。
- 键盘技能矩阵实现状态需先核对 `objects.ts` 与 `desk.ts`。
- 校奖/院级默认隐藏，别在「全部」里漏掉隐藏逻辑。

---

## 完成后 commit 信息

```
Phase 5 精修：细节物件（键盘技能矩阵 + 证书墙 + 本子 Links + 台灯主题）
```
