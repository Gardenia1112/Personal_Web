# 移动端紧急修改 · 最终版计划（v3）

> **适用范围**：`lszbf.site` 全站移动端（手机 `<640px`），平板与桌面（`>=900px`）保持现有设计不变。
> **执行方式**：喂给 Claude，按编号从 ⑦ 开始逐项实现，**做完一项、本地 375px 验收一项**，再进下一项。
> **编号说明**：严格对应你发来的截图文件夹 ① ③ ④ ⑤ ⑥ ⑦ ⑧ ⑨ ⑩（**② 你本轮未列出，留空不虚构任务**）。

---

## 〇、全局约定（所有任务共同遵守）

1. **断点**：手机 `<640px` 单写，平板+桌面 `>=900px` 共用；全局 token 收敛为 `--bp-sm:640px / --bp-md:900px`。
2. **交互**：移动端一律用 `pointerup`（同时覆盖 touch 与 mouse），**不再只绑 `mouse*`**；可交互元素必须有 `aria-label` + `role`。
3. **动效降级**：`@media (prefers-reduced-motion: reduce)` 与 `@media (pointer: coarse)` 下，关闭自定义光标、磁吸、视差。
4. **触摸目标**：可点击区域最小 `44×44px`。
5. **性能**：大文件（`.glb`/`.cs`/PDF）一律懒加载、按需加载、点击才触发下载。
6. **改法**：Claude 每改一个文件**先读后改**、最小改动、单独 commit；禁止大范围重写。

---

## ① 绘画作品目录 · 排版与大小

**截图**：`01-绘画作品目录.png`（art/index 列表页）

**问题**：卡片尺寸与间距在窄屏错乱，图过大/文字溢出，网格未单列。

**改动**：
- `src/pages/art/index.astro`
- `src/components/ArtGallery/*.astro` + `ArtPieceCard.astro`
- `src/styles/`（art 列表相关 CSS）

**做法**：
1. 移动端（`<640px`）改为**单列竖向卡片**，图占满宽、标题/标签在图下。
2. 卡片网格：`grid-template-columns: repeat(auto-fill, minmax(280px, 1fr))` → 窄屏强制 1 列。
3. 缩略图固定宽高比 `aspect-ratio: 4/3`，`object-fit: cover`，避免拉伸。
4. 字体/间距改用 `clamp()`，禁止固定 `px` 挤占。
5. 点击整卡进详情，移除 hover 才显示的悬浮按钮。

**验收**：375px 下单列、无横向滚动、图不裁切变形、标签可读。

---

## ③ 气垫导轨详情页 · 文案改"作品展示" + 文字适配

**截图**：`03-气垫导轨文案.png`

**问题**：该 section 标题"分工边界"需改为"作品展示"；内容要改成**简介作品**；**桌面端与移动端共用同一份文案**，仅字号响应式。

**改动**：
- `src/data/projects.ts` → 找到气垫导轨条目，新增/修正字段：
  - `sectionTitle: "作品展示"`（原"分工边界"）
  - `showcase`（数组）：每项是 `{ subtitle, body }` 简介段落
- `src/pages/works/[slug].astro`（气垫导轨详情页模板）
- `src/styles/project-detail.css`

**做法**：
1. 将"分工边界"全部替换为"**作品展示**"（含目录树里的 `CH 03 我的贡献` 章节标题，若该页复用同一数据，一并改为"作品展示"）。
2. **文案唯一来源 = `projects.ts`**，模板读取 `project.showcase` 渲染；桌面与移动端**用同一份数据**，仅通过 `clamp()` / `@media` 调整字号行距。
3. 文字：`body` 字号 `clamp(15px, 4vw, 17px)`，行高 `1.7`；标题随断点缩放。
4. 视频播放器：宽 100%、`aspect-ratio` 保比例，控件不被裁。

**验收**：桌面 + 375px 显示同一份简介、无横向溢出；"分工边界"字样彻底消失。

---

## ④ 智联传感详情页 · 移动端点击交互

**截图**：`04-智联传感.png`

**问题**：图标点击无反应（屏幕不适配）。移动端改为 **点击图标 → 弹出对应介绍；点返回键 → 回到选择页**。

**改动**：
- `src/data/projects.ts` → 智联传感条目，确保 4 个模块数据结构：`{ id, icon, title, description }`（监测台/数据采集/运行日志/归档）。
- `src/pages/works/[slug].astro` 或对应详情组件
- `src/scripts/desktop/*.ts`（该页交互脚本）

**做法**：
1. 移动端（`<640px`）：渲染**图标网格**（4 个模块），每个 `role="button" tabindex="0"`。
2. 点击图标 → 打开**浮层（modal）**展示该模块 `title + description`；浮层顶部有**"返回 / ←"按钮**。
3. **返回键逻辑**：
   - 浮层内"返回"按钮 → 关闭浮层回选择页；
   - 浏览器原生返回（`popstate`/`history.back`）→ 若浮层打开则先关浮层，再返回上一页；
   - 用 `pointerup` 统一绑定，区分"打开"与"关闭"。
4. 桌面端（≥900px）保留原 hover/点击行为，不受影响。
5. 路由：`#module=xxx` 可深链，刷新保持。

**验收**：375px 点图标弹介绍、点返回关闭；原生返回键逐层退出；桌面端原交互不变。

---

## ⑤ 不绘鸽工作室详情页 · 排版 + 点击弹出/任意处关闭

**截图**：`05-不绘鸽工作室.png`

**问题**：排版乱；交互改为 **点击卡片弹出详细介绍，再次点击任意处关闭**。

**改动**：
- `src/data/projects.ts` → 不绘鸽条目，各模块/卡片 `{ id, title, detail }`
- 详情页模板 + 对应脚本

**做法**：
1. 排版：便签/卡片改为单列堆叠，图钉装饰保留但缩小；固定 `px` 改 `clamp()`。
2. **弹出**：点击任一卡片 → 打开详情浮层（标题 + 完整 `detail`）。
3. **关闭（关键，区别于 ④）**：`pointerup` 绑定在**浮层背景 + document**——浮层打开后，**再次点击页面任意处**（含背景、其他卡片）即关闭；同时保留显式关闭按钮与 Esc 键。
4. 用状态锁 `isOpen` 防重复触发；`stopPropagation` 只在点击浮层内容区时阻止关闭。

**验收**：点开 → 点空白/其他卡片/卡片本身 → 关闭；点内容区不误关；Esc 可用。

---

## ⑥ 流浪尸潮详情页 · 大改（移动端滑动页 + 电脑端优化）

> 本节基于真实数据：`src/data/projects.ts`（tech 字段）+ `src/data/langshi-ch3/code/`（约 60 个 `.cs`，分 Core/Abilities/Enemy/HP/Events/GameSystem/Managers/UI/Audio/Animation/Statistics/Others）+ 详情页 **CRT 显示器 4 频道**结构。

### 6.1 项目事实（Claude 实现依据）

**技术**（来自 `projects.ts` 的 `tech`）：
- C# 面向对象 + 组件化架构
- 泛型状态机 FSM —— `Core/Character/CharacterStates.cs`
- 事件驱动解耦 —— `Events/GameEvents.cs`（战斗/UI/经济）
- 数据驱动 —— ScriptableObject（肉鸽"三选一"、动态难度、局外成长）
- 对象池 —— `Others/ObjectPool.cs`（降 GC）
- 2.5D 渲染 —— URP Decal 动态阴影、`Others/Background_Parallax.cs` 视差

**成果**：平均帧率 35→60 FPS（+71%）、内存峰值 −18%；Gitee 源码 `Unity_Game2025`。

**详情页 = CRT 显示器，4 频道**：
| 频道 | 内容 |
|---|---|
| CH-01 信号 | 封面图 |
| CH-02 实机 | 游戏实机视频（mp4） |
| CH-03 制作 | 真实 C# 源码滚动浏览 + 概念图；5 个重点骨架置顶（CharacterStates / Core 角色核 / GameEvents / Ability_Base 能力基类 …） |
| CH-04 记录 | 四章文字（01问题 / 02技术方案 / 03我的贡献 / 04量化成果）+ 帧率/内存量化条形图 + Gitee 链接 |

代码目录可见游戏内容：`Character`（Move/Jump/Dash/ShootATK）、敌人与刷怪（Enemy/EnemySpawner）、经济成长（Currency/Exp/Equipment/Upgrade）、成就（Achievement）、音频（AudioManager）。

### 6.2 卡顿根因（决定优化方向）

| 主因 | 证据 | 优化 |
|---|---|---|
| **① shiki 一次性高亮 60 个 .cs** | CH-03 源码滚动 + 概念图 | **懒加载 + 按需高亮**：仅高亮当前可见代码块；用 IntersectionObserver 进入视口才高亮；或分页/折叠，首屏不高亮全部 |
| **② 视差背景每帧更新** | `Background_Parallax.cs` | 降配：`prefers-reduced-motion` / 移动端**关闭视差**或改为静态分层图 |
| **③ URP Decal 动态阴影** | 2.5D 渲染 | 移动端降级为烘焙阴影 / 关闭 Decal |
| ④ 像素动画 | CRT 风格 | 精灵图合并 + `will-change` + 离屏停止动画 |

> **默认选择（已在文档标注，你可推翻）**：优化重心 = **① shiki 懒加载**（收益最大）+ **②③ 移动端降级**；对象池 `ObjectPool.cs` 已优化，**不动**。

### 6.3 移动端（`<640px`）：普通滑动展示页

**改动**：
- `src/pages/works/[slug].astro`（流浪尸潮详情）
- `src/scripts/desktop/*.ts`（该页脚本，**移动端整文件不加载**）
- `src/styles/` + 新增 `src/styles/langshi-mobile.css`

**做法**：
1. **门控**：`if (window.innerWidth < 640) → 走滑动页，不 import desktop/*.ts`（解除 3D/拖拽/滚轮依赖）。
2. **保留 CRT / 像素风**（视觉风格不变）：黑底 + 像素字体 + 绿/红立体字（如 CH-01 信号、Crisis Live）。
3. **4 频道 → 4 个竖向 Section**（普通文档流，可滑动）：
   - `section-ch01` 封面图
   - `section-ch02` 实机视频（`aspect-ratio`、懒加载、`preload="none"`）
   - `section-ch03` 源码：**默认折叠**，点"查看源码"才懒加载 shiki 高亮；5 个骨架用**卡片网格**展示（不再横向滚动）
   - `section-ch04` 记录：四章文字**点击展开**（同 ⑧ 手风琴）+ 帧率/内存条形图（纯 CSS）+ Gitee 链接
4. shiki：**代码块按需高亮**，进入视口才执行；长文件截断"展开全部"。

**验收**：375px 可竖向滑完 4 个 Section、不卡顿（60fps 目标）、无横向滚动；视频/音频不自动播；源码不一次性高亮。

### 6.4 电脑端（≥900px）：保留 CRT 4 频道，优化性能

- shiki 高亮**虚拟化/分页**，仅高亮当前频道代码；切换频道再高亮对应块。
- 视差 `Background_Parallax`：`requestAnimationFrame` 节流，离开视口 `pause`。
- Decal 动态阴影：加"画质"开关，默认中档。
- 对象池、状态机、事件系统**保持不动**（已是优化项）。

**验收**：桌面端 4 频道切换流畅，帧率维持 60 FPS，内存不涨。

---

## ⑦ PDF / 文件自动下载 · 全局修复（最紧急）

**截图**：`07-获奖页下载.png`（含微信"离开"提示）

**问题**：获奖页一打开就**强制下载文件**；**所有含 PDF/文件的页面都如此**（全局问题）。

**根因**：页面加载即执行 `location.href=xxx.pdf` / 创建 `<a download>` 触发 `.click()` / 或 `download` 属性 + 自动脚本。

**改动（全局排查）**：
- 全局搜索以下关键词，**逐个修复**：
  - `\.pdf` `location.href` `window.open(...pdf)`
  - `a.click()` `dispatchEvent` + `download`
  - `autoDownload` `onload=...download`
- 涉及文件（示例，以搜索结果为准）：
  - `src/pages/awards.astro` + `AwardsStack.astro`
  - `src/pages/works/[slug].astro`、`src/pages/art/[slug].astro`
  - 任何含 `download` 属性的模板
  - `src/scripts/*.ts`（自动下载逻辑）

**做法**：
1. **所有下载改为用户点击才触发**：`<a href="...pdf" target="_blank" rel="noopener">预览</a>`，**移除 `download` 属性**（让浏览器预览而非下载）。
2. 确需下载的（如简历 PDF）：用**显式按钮** `"下载 PDF" → onclick 才设置 `download` 并触发。
3. **禁止在 `onload` / 首屏脚本里自动调用下载**。
4. 移动端 PDF 用 `<iframe>` 或新标签页预览，避免跳转微信"离开"提示。

**验收**：清空缓存重新打开获奖页 / 各作品页 / 关于页 → **无任何文件自动下载**；点"预览"才打开，点"下载"按钮才下载。

---

## ⑧ 关于我页面 · 01-04 点击展开

**截图**：`08-关于我.png`

**问题**：01/02/03/04 四段改为**点击展开**，文案 = 电脑端的简介。

**改动**：
- `src/pages/about.astro`
- `src/data/about.ts`（新增，存放 01-04 简介文案；如已有数据文件则复用）
- 对应脚本（改为手风琴，移除 hover 展开）

**做法**：
1. 数据结构 `sections: [{ id:'01', title, summary, detail }]`，`detail` = 电脑端现有简介文案（**同一份，仅移动端默认折叠**）。
2. 桌面端（≥900px）：默认全部展开（保留现有排版）。
3. 移动端（`<640px`）：**默认折叠**，点标题 → 展开/收起（手风琴，可同时展开多个或单选，默认单选）。
4. 用 `pointerup` + `aria-expanded` + `role="button" tabindex="0"`；**内容用 `max-height` / `grid-template-rows` 过渡**。
5. 详情文案**来自同一数据源**，确保桌面与移动端文字一致。

**验收**：375px 点 01 → 展开简介，再点收起；四个独立；桌面端仍全展开。

---

## ⑨ 主页 · 重做风格（2D + 保留物件映射，不用 3D 工位）

**截图**：`09-主页1.png`（`06-流浪尸潮` 那张是误归入，主页另见清爽版截图）

**决策（已确认）**：分区 = **作品 / 关于 / 获奖 / 联系**（4 个，正确）。

**改动**：
- `src/pages/index.astro`（首页）
- `src/components/Desk3D/Scene.astro` → **首页不再渲染 3D 工位**（`desk.ts` 在首页不加载 / 或保留但降级为静态 fallback）
- `src/scripts/desk.ts` → 首页入口移除，仅 `/desktop` 按需保留
- 新增 `src/styles/home-2d.css`（清新拟物风）

**风格（2D 清新拟物，保留物件→分区映射）**：

| 物件（图标） | 映射分区 | 点击去向 |
|---|---|---|
| 📁 文件夹 | 作品（Works + Art） | `/works` |
| 👤 人物 | 关于我 | `/about` |
| 🏆 奖杯 | 获奖与证书 | `/awards` |
| ✉️ 信封 | 联系我 | `/contact` |

**做法**：
1. **移除 3D 工位**：首页不加载 `desk.ts` 的 Three.js（删除 `initDesk` 调用或加页面守卫），**保留 `/desktop` 页的 3D（你另有说明则按需）**。
2. **2D 主页**：浅蓝白背景、圆角**拟物卡片**（柔和阴影）、手写体装饰字（welcome）。
3. **4 个物件卡片**：网格 `2×2`（移动端单列），每卡 `role="link"`，点击跳转对应分区。
4. 保留顶部欢迎语、姓名/副标题（游戏制作人），排版精简。
5. 物件映射数据集中一处（`homeLinks`），方便维护。

**验收**：首页 2D、无 3D 卡顿、清爽；4 个物件可点进对应页；移动端单列无溢出。

> ⚠️ **待你确认（默认已定）**：物件映射分区是否就这 4 个（作品/关于/获奖/联系）？需增减告诉我。当前按你确认的 4 分区实现。

---

## ⑩ 全局导航 · 汉堡抽屉（C1-a）

**截图**：`10-导航.png`（导航栏排版过大 + 动效错位）

**改动**：
- `src/components/LineMenu.astro`（导航主体）
- 新增 `src/components/LineMenuMobile.astro`（或在同一文件内条件渲染）
- `src/styles/line-menu.css`

**做法**：
1. **窄屏（`<640px`）**：
   - 顶部栏**缩小字号、精简**（仅留 Logo + 汉堡按钮 ☰），不再横铺全部链接。
   - 汉堡按钮 → 打开**侧滑抽屉**（从右/左滑入），含全部导航项 + 联系我高亮。
   - 抽屉用 `pointerup` 打开/关闭、Esc 关闭、点击遮罩关闭、`focus-trap`、`aria-expanded`。
2. **动效修正**：Alonso 风格动效**仅桌面保留**；移动端抽屉用简洁 transform 过渡，`prefers-reduced-motion` 下取消。
3. **桌面（≥900px）**：完全保留现有 LineMenu 横向 + Alonso 动效，不动。
4. 导航数据统一来源（如 `src/data/nav.ts`），避免两处维护。

**验收**：375px 汉堡可开关、链接可点、遮罩/Esc 可关、无动效错位；桌面端原样正常。

---

## 文件改动清单（汇总）

| 编号 | 文件 | 动作 |
|---|---|---|
| 全局 | `src/styles/tokens.css`（新增）+ `global.css` | 加 `--bp-sm/md` |
| ① | `src/pages/art/index.astro`、`ArtPieceCard.astro`、art CSS | 单列卡片 |
| ③ | `src/data/projects.ts`、`works/[slug].astro`、`project-detail.css` | "作品展示"文案 |
| ④ | `projects.ts`（智联模块）、详情组件、`desktop/*.ts` | 弹层+返回 |
| ⑤ | `projects.ts`（不绘鸽）、详情组件、脚本 | 弹层+任意处关 |
| ⑥ | `works/[slug].astro`、`desktop/*.ts`、`langshi-mobile.css`、shiki 配置 | 滑动页+性能 |
| ⑦ | **全局**：`awards.astro`、`AwardsStack.astro`、`[slug].astro`×2、含 download 的脚本 | 移除自动下载 |
| ⑧ | `about.astro`、`src/data/about.ts`、脚本 | 手风琴 |
| ⑨ | `index.astro`、`Scene.astro`、`desk.ts`（首页入口）、新增 `home-2d.css` | 2D 主页 |
| ⑩ | `LineMenu.astro`、新增移动菜单、line-menu.css | 汉堡抽屉 |

---

## 执行顺序

```
⑦ PDF止血（全局优先）
  → ⑩ 汉堡导航（全站壳）
  → ⑨ 主页（2D 物件映射）
  → ① 绘画目录
  → ③ 气垫导轨文案+字号
  → ④ 智联传感弹层
  → ⑤ 不绘鸽弹层
  → ⑥ 流浪尸潮滑动页+性能
  → ⑧ 关于我手风琴
  → 全局收尾（断点收敛、reduced-motion）
```

**⑦⑩⑨ 先做（全局壳），其余页面级可并行。**

---

## 验收清单（最终）

- [ ] **⑦** 获奖/作品/关于页打开**不再自动下载**任何文件
- [ ] **⑩** 375px 汉堡抽屉开关正常、桌面导航原样
- [ ] **⑨** 首页 2D、清爽、4 物件可跳转、无 3D 卡顿
- [ ] **①** 绘画列表单列、图不变形
- [ ] **③** "分工边界"→"作品展示"彻底消失，桌面/移动文案一致
- [ ] **④** 智联传感：点图标弹介绍、返回键回选择页
- [ ] **⑤** 不绘鸽：点弹详情、任意处再点关闭
- [ ] **⑥** 流浪尸潮：375px 竖向滑完 4 Section、60fps、无横向滚动；桌面端流畅
- [ ] **⑧** 关于我 01-04 点击展开、文案同桌面
- [ ] 全站无横向滚动、触摸目标 ≥44px、`prefers-reduced-motion` 下降级生效

---

## Claude 执行提示（写在最前）

1. 每任务**先读相关文件**、确认现有结构再改，禁止臆造类名。
2. **先修 ⑦**（自动下载是全站阻塞问题），再 ⑩⑨（全局壳），最后页面级。
3. **数据驱动**：文案/模块统一放 `src/data/*.ts`，模板读取，**桌面与移动共用一份数据**。
4. **门控降级**：`if (innerWidth < 640)` 跳过重型脚本（3D、shiki、视差、desktop/*.ts）。
5. **交互统一**：`pointerup` + `aria` + `role` + 显式关闭；区分 ④（返回键）与 ⑤（任意处关）。
6. **流浪尸潮（⑥）** 严格按"CRT 4 频道 → 4 Section"结构，保留像素风，**只改布局与加载方式**，不改视觉风格。
7. 每完成一项，本地 `npm run dev` + 浏览器 375px + 触控模拟**逐项验收**，再 commit。
8. **不要**为了"好看"擅自改设计风格；风格以本文件 ⑨ 与⑥的设定为准。

---

**v3 更新点**：⑥ 流浪尸潮基于真实 `projects.ts` + `langshi-ch3/code/` + CRT 4 频道重写（含卡顿根因与优化映射）；⑨ 确认 4 分区物件映射；③④⑤⑧ 交互差异明确。
