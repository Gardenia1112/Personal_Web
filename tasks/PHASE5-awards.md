# PHASE 5：获奖证书页（文件夹 → /awards）

> 阶段标签：Phase 5（证书墙）

---

## 目标

点击首页文件夹进入 `/awards`，展示获奖证书墙，按类别分组（竞赛获奖/证书/待补），证书可预览。**奖学金类别已整体删除。**

---

## 对应文档

- `docs/01-内容结构文档.md` §6（奖项与证书内容）
- `docs/02-视觉交互设计系统.md` —— 证书墙视觉定稿（**正文勿改**）
- `docs/03-技术架构文档.md` §5.3（awards.ts 字段）、§9（证书素材待补）

---

## 入场状态

✅ **已完成**。代码：`src/pages/awards.astro` + `src/data/awards.ts`。

- 奖学金类别与「奖学金」字样已删除；分组改为 全部/竞赛获奖/证书/待补。
- 证书图片已英文命名，路径 `/assets/awards/`；CIMC 条目已补 `2025-cimc-siemens-2nd.jpg`。
- 本卡是**精修**，重点是素材补位与隐藏开关。

---

## 执行步骤（精修项）

1. **证书素材补位**：核对 `awards.ts` 里 `file` 占位项，把实际扫描件放进 `public/assets/awards/`，英文命名。
2. **校级/院级隐藏开关**：`Award.hide?` 字段控制「校级院级」默认隐藏，确认开关生效（用户要求：校奖/院级奖可隐藏）。
3. **分类分组**：确认 `category`（省级及以上/校级院级/待补/证书）分组渲染正确，无漏项。
4. **创新大赛以一等奖为准**：确认该条信息（用户明确：以一等奖为准）。
5. **数模省一占位**：全国大学生数学建模竞赛辽宁省一等奖 = 占位，等用户补证书，标 `⚠️ 待确认`。
6. **预览交互**：证书点击预览（弹窗/放大）正常，路径用 `/assets/awards/` 绝对路径（不 `encodeURI`）。

---

## 验收标准

- [ ] `/awards` 正常渲染，分组正确，奖学金已彻底移除。
- [ ] 已有证书图片可预览，无断图。
- [ ] 校级/院级隐藏开关生效。
- [ ] 创新大赛显示一等奖。
- [ ] `npm run build` 通过。

---

## 风险与提醒

- ⚠️ **证书扫描件待用户提供**：`file` 为占位的条目先标 `⚠️ 待确认`，别编造文件路径。
- 数模省一是占位证书，用户会补，先保留占位但明确标注。
- 校奖/院级默认隐藏，别在「全部」里漏掉隐藏逻辑。

---

## 完成后 commit 信息

```
Phase 5 精修：证书墙素材补位与隐藏开关
```
