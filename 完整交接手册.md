# 完整交接手册：Claude Code → Cursor

> **本手册 + 01 + 02 + 03 四份文件，构成你现在到上线的全部依据。**
> 你已经完成：**Claude Code 生成了 Phase 1-8 的代码骨架**（即 03 §4 的文件结构已落地）。
> 你现在处于：**准备把项目交给 Cursor 做 3D/动效微调的阶段**。
>
> 本手册只解决一件事：**怎么把接力棒干净地交给 Cursor，让它不跑偏、不返工、不覆盖 Claude 的成果。**

---

## 一、你之前的疑问：这跟之前给的方案一致吗？

**结论：核心思路一致（教程方法论），但旧文件有 3 处已过期，本版已全部修正。**

### ✅ 一致的（不用改）
- **工具分工哲学**：Claude Code = 管「想 + 搭」（架构/Plan/重构/部署），Cursor = 管「看 + 调」（3D/动效/像素级复刻/微调）—— 完全对齐教程（Codex 出 Plan + Cursor 微调）
- **交接本质**：不是"直接问 Cursor"，而是**先喂交接包再开工**
- **防覆盖**：切换工具前先 `git commit`；不同时操作同一批文件

### 🔴 已修正的 3 处不一致（对照旧文件）
| # | 旧版问题 | 本版修正 |
|---|---------|---------|
| 1 | **Phase 编号双套**：02 §10 用 Phase 1-8，旧手册用 Phase 3-6 | **统一为 Phase 1-8**（见 §四） |
| 2 | **03.md 从未生成**，你无法对照三份文档 | 已补出 `03-技术架构文档.md`（技术栈唯一权威） |
| 3 | 旧任务卡只认「流浪尸潮/不绘鸽」2 个项目 | 按最新 01 §4，**项目 = ×4**（新增智联传感、气垫导轨） |

### 📦 你现在应该有的文件清单（核对一下）
```
/data/workspace/
├── 01-内容结构文档.md        ← 你提供的（最新）
├── 02-视觉交互设计系统.md     ← 你提供的（最新）
├── 03-技术架构文档.md        ← 本次新增（技术唯一权威）
├── AGENTS.md                 ← 本次重写（Cursor 开机说明书）
├── PROJECT_STATE.md          ← 本次生成（进度快照）
├── 完整交接手册.md           ← 本文件
└── tasks/
    ├── PHASE1-2-工位骨架与开场.md
    ├── PHASE3-游戏化介绍.md
    ├── PHASE4-桌面OS.md
    ├── PHASE5-细节物件.md
    ├── PHASE6-彩蛋与优化.md
    └── PHASE7-内容填充与部署.md
```

> 如果某个文件你本地没有，**就让 Claude Code 生成**（见 §三 Step 1），别自己手写。

---

## 二、交接包结构（放项目根目录 `handoff/`）

```
your-project/
├── handoff/                    ← 全部交接文件集中于此
│   ├── AGENTS.md              ← ⭐ Cursor 第一句话就读它
│   ├── PROJECT_STATE.md       ← 当前进度快照（本次已填到 Phase 7）
│   ├── references/            ← 你找的参考（Cursor 能读图/开 URL）
│   │   ├── pinterest/         ← 工位灵感截图
│   │   ├── codrops/           ← 动效链接 + .md 说明
│   │   ├── reactbits/         ← 窗口系统源码参考
│   │   └── recordings/        ← 动效录屏（5-10s）
│   └── tasks/                 ← 按 Phase 拆的任务卡（§四）
│       ├── PHASE1-2-工位骨架与开场.md
│       ├── PHASE3-游戏化介绍.md
│       ├── PHASE4-桌面OS.md
│       ├── PHASE5-细节物件.md
│       ├── PHASE6-彩蛋与优化.md
│       └── PHASE7-内容填充与部署.md
│
├── src/                        ← Claude Code 已生成的代码（别动结构）
├── public/素材/                ← 真实素材（01 §4/§6）
├── models/                     ← 3D 物件（img2three.js 生成）
├── package.json
├── astro.config.mjs
└── git 仓库（已 commit）
```

### 各文件职责（谁读、谁写）
| 文件 | 给谁读 | 谁负责维护 |
|------|-------|-----------|
| `AGENTS.md` | Cursor（每次新对话第一句） | 你/Claude，结构稳定后少改 |
| `PROJECT_STATE.md` | Cursor（接手即看进度） | **每个 Phase 完成 commit 后更新** |
| `tasks/PHASE*.md` | Cursor（一次只读一张） | Claude Code 生成初版，Cursor 微调 |
| `references/` | Cursor（看图/开链接） | **你手动放**（参考在你本地浏览器） |

> ⚠️ **参考素材只能你手动放**：Pinterest 截图、动效录屏、Codrops 链接都在你本地，Claude Code 不联网读不了，Cursor 在本地能读。**这是你唯一必须手动的核心动作。**

---

## 三、Step by Step：从"Claude 做完"到"Cursor 接手"

> 每一步标了 **[手动]** 或 **[交给 AI]**，[手动] 我会写到"按哪个键"级别。

### Step 1：基线 commit（[交给 Claude Code]）
**前置**：打开 Claude Code，**在 Claude 生成的项目根目录**启动它。

把这段整块发给 Claude Code（照抄，一句不改）：
```
我要在当前项目做 Git 初始化并打一个基线 commit，但我之前操作失败了。
请你帮我完成以下全部步骤，每执行一步都把命令和输出告诉我：

1. 检查是否安装 Git：git --version
2. 检查当前目录：pwd（确认是项目根目录，有 package.json 和 src/）
3. 检查是否已是 git 仓库：ls -la（看有没有 .git）
4. 如果未初始化：git init
5. 检查/配置用户名邮箱（先查全局：git config --global user.name，
   没有再让我提供；配置用：git config user.name "..." / user.email "..."）
6. git add .
7. git commit -m "baseline: Claude Code Phase 1-8 complete"
8. 验证：git log --oneline -1
9. 然后**读取以下 4 个文件，在 handoff/ 目录生成对应的空白任务卡骨架**：
   - /data/workspace/01-内容结构文档.md
   - /data/workspace/02-视觉交互设计系统.md
   - /data/workspace/03-技术架构文档.md
   - /data/workspace/完整交接手册.md
   依据它们把 tasks/PHASE1-2 ~ PHASE7 六张卡写成初版（内容见手册 §四），
   放进项目根 handoff/tasks/。
10. 最后告诉我：commit 哈希、handoff/ 目录结构、以及是否可以安全交给 Cursor。

任何一步报错先停下，把完整错误信息给我诊断。
```

Claude 会停在"请提供用户名邮箱"——你回：
```
名字：赵韵婷
邮箱：m19967792313@163.com
```
（用 GitHub 绑定邮箱更好，二选一）

**成功标志**（Claude 最终输出）：
```
[main (root-commit) abc1234] baseline: Claude Code Phase 1-8 complete
 142 files changed, 8932 insertions(+)
```
记下哈希 `abc1234` —— 这是你的**救命备份点**。

> 若 Claude 报 `git: command not found`：说明没装 Git，**只能手动**（见下）。

### Step 1a（仅当没装 Git）：装 Git [手动]
- **Windows**：进 `git-scm.com/download/win` → 下载 → 双击 → **一路 Next，全部默认**（务必勾选 "Add to PATH"）→ 装完**重启终端** → 回到 Step 1 让 Claude 继续
- **Mac**：终端输 `git`，弹窗点"安装命令行工具"
- 装完验证（[手动]，在终端敲）：
  ```
  git --version
  ```
  出现 `git version 2.x.x` 即成功。

### Step 2：让 Claude 生成交接文件（[交给 Claude Code]）
承接 Step 1 的第 9 步，如果 Claude 没自动做，就单独发：
```
请基于 01/02/03 三份文档 + 当前项目实际代码（读 src/、package.json），
生成以下文件（已存在则覆盖更新）：
1. handoff/AGENTS.md       —— 开机说明书（模型分工、绝对规则、参考喂法）
2. handoff/PROJECT_STATE.md —— 进度快照（已完成 Phase 1-8 骨架，当前待交接 Cursor）
3. handoff/tasks/PHASE1-2 ~ PHASE7 六张任务卡（对齐 03 §10 的 Phase 划分）
4. 把 /data/workspace/ 的 01/02/03 + 完整交接手册 + AGENTS + PROJECT_STATE
   复制到项目根 handoff/docs/ 作为 Cursor 的只读参考副本。

生成完列出 handoff/ 完整目录树给我确认。
```
> 💡 本版已帮你写好 `AGENTS.md` 和 `PROJECT_STATE.md`（见 §五、§六），你可以让 Claude **直接用这两份**，或让它在此基础上调整。

### Step 3：放入参考素材（[手动] —— 唯一必须你做的核心动作）
1. 在文件管理器打开项目根目录的 `handoff/references/`
2. 把你在 Pinterest / Codrops / Reactbits / B 站录屏的素材按文件夹放好：
   - 工位构图截图 → `pinterest/`
   - 动效链接写成 `.md`（含链接 + 一句话"复刻哪部分"）→ `codrops/`
   - 窗口系统参考 → `reactbits/`
   - 动效录屏（5-10s）→ `recordings/`
3. **每个参考写一个 `说明.md`**：链接 + 截图文件名 + "我要复刻的具体部分"。这是给 Cursor 的关键上下文。

**喂法示例**（后面 Cursor 对话里直接用）：
> 拖入截图 + 说：`参考 handoff/references/codrops/folder-interaction.md 和这张截图，把电脑桌面的文件夹打开动画做成这样，1:1 复刻运镜和缓动。`

### Step 4：打开 Cursor（[手动]）
1. **关闭 Claude Code**（避免同时改同一批文件）
2. 打开 Cursor → `File → Open Folder` → **选中项目根目录**（不是 `handoff/`）
3. 底部终端确认在根目录：`pwd` 应显示项目路径
4. **切模型**（[手动]，3 次点击）：
   - 输入框上方模型下拉 → 选 **`Claude Opus 5 High`**（Phase 3/4 用最强）
   - 轻量微调可换 `Claude Opus 4.8 High` 或 `Composer 2.5 Fast`

### Step 5：开机指令（[手动] —— 每次新对话第一句）
在 Cursor 新对话，**先发这段**（不要直接让它写代码）：
```
请按顺序完整阅读以下文件（不要写代码）：
1. ./handoff/AGENTS.md
2. ./handoff/PROJECT_STATE.md
3. ./handoff/docs/03-技术架构文档.md （技术事实以它为准）
4. ./handoff/tasks/PHASE3-游戏化介绍.md

读完后，用 5 行以内告诉我：
- 当前处于哪个 Phase
- 已完成了什么
- 接下来要做哪 3 件事
- 你（Cursor）打算用什么模型/方案
```
> 💡 **逼它复述、不许写代码** —— 复述错了当场纠正，成本极低。这是交接的灵魂。

### Step 6：复述正确 → 发任务卡（[交给 Cursor]）
```
阅读 ./handoff/tasks/PHASE3-游戏化介绍.md 全文。
先输出一份 mini-plan（不超过 10 行）：实现顺序 + 涉及文件 + 风险点。
我确认后再写代码。
```
确认 mini-plan → 说 `开始执行` → Cursor 写代码。

### Step 7：看效果（[手动]）
```bash
npm run dev
```
浏览器打开 `http://localhost:4321`（Astro 默认端口，以终端输出为准）。
**改一行 → 保存 → 浏览器自动刷新**（Cursor 实时预览优势）。

### Step 8：完成一个 Phase → 更新状态 + commit（[手动]）
```bash
git add -A
git commit -m "phase3: 游戏化介绍界面 A/D 操控 + 4 展板"
```
然后更新 `PROJECT_STATE.md` 把 Phase 3 标 ✅（让 Claude/Cursor 帮你改也行）。

---

## 四、Phase 划分（⭐ 已统一，02 §10 + 03 §10 一致）

| Phase | 目标 | 验收（对齐 03 §9） | 主工具 | 对应任务卡 |
|-------|------|------------------|--------|-----------|
| **1-2** | 工位骨架 + 开场剧本 | 静态 3D 桌面 + 6 P0 物件 + hover 标题 + 开场剧本 ①-⑧ 播完一次 | Claude Code（骨架）→ Cursor（调 GSAP） | `PHASE1-2-工位骨架与开场.md` |
| **3** | 🎮 游戏化 About | 横版小人 + A/D/鼠标三输入 + 4 展板（来自 01 §3）+ ESC 返回 | **Cursor**（Phaser） | `PHASE3-游戏化介绍.md` |
| **4** | 💻 桌面 OS | Works/Blog/Resume 窗口 + 可拖拽 + 复用旧 UI 代码 | **Cursor**（React + Reactbits） | `PHASE4-桌面OS.md` |
| **5** | 细节物件 | 键盘技能矩阵、证书翻页墙、本子 Links、台灯主题 | Cursor | `PHASE5-细节物件.md` |
| **6** | 彩蛋 + 优化 | 咖啡/耳机/贴纸彩蛋 + 移动端降级 + 性能达标 | Cursor | `PHASE6-彩蛋与优化.md` |
| **7** | 内容填充 + 部署 | 01 真实素材灌入 + Cloudflare Pages + 自定义域名 + HTTPS | Claude Code（部署）+ Cursor（填数据） | `PHASE7-内容填充与部署.md` |

> 📌 **你当前位置**：Claude Code 已完成 Phase 1-8 骨架（= 03 §4 文件结构落地），**准备交接 Cursor 做 Phase 3-6 的细节与动效**。Phase 1-2 若需调优也归 Cursor。

---

## 五、模型分工（对应你的 Cursor 下拉选项）

| 场景 | Cursor 模型 | 理由 |
|------|------------|------|
| Phase 3 游戏界面（状态机/三输入） | **Claude Opus 5 High** | 最复杂，要长上下文 |
| Phase 4 桌面 OS（跨多文件） | **Claude Opus 5 High** | 复用旧代码、逻辑多 |
| Phase 5 证书/本子页 | **Claude Opus 4.8 High** | 偏视觉还原 |
| Phase 6 微调/动效参数 | **Opus 4.8 High**（试参换 Fast） | 看图为主 |
| 单行补全/报错问答 | **Composer 2.5 Fast** 或 **Cursor Grok 4.5 High Fast** | 轻量省配额 |

**切模型 3 步**：输入框上方 → 模型下拉 → 点选。**口诀**：开新 Phase 用 Opus 5 High，改一行试参用 Fast，不确定永远 Opus High。

> 截图里的具体版本号会随 Cursor 更新变化 —— 认 **"Claude Opus" + "High"** 就是最强的，不用纠结精确号。

---

## 六、防覆盖规则（⚠️ 必守）

1. ❌ **Claude Code 与 Cursor 不同时开同一项目**
2. ✅ **切换工具前先 `git commit`**（每个 Phase 一个 commit）
3. ✅ **一个 Phase = 一个新 Cursor 对话**（上下文干净、省 token）
4. ✅ **改崩了回滚**：`git reset --hard <commit哈希>` 回到备份点
5. ✅ **动效/3D/像素复刻/穿模 → Cursor**；**架构重构/部署/跨多文件 → Claude Code**

---

## 七、何时交回 Claude Code（信号）

出现以下情况，就**关闭 Cursor → 开 Claude Code**：
- 需要跨 3+ 文件的大重构
- 改了半天 Type 报错越来越多
- 要接 Cloudflare Pages Functions / 环境变量 / 构建配置
- 要部署上线（Git 推送、Cloudflare 配置）

交回时也用交接包：更新 `PROJECT_STATE.md` + 新任务卡，Claude 读后执行。

---

## 八、救命后路（记住这 3 条命令）

```bash
git status                    # 看当前改了啥
git log --oneline -10         # 看历史 commit
git reset --hard <哈希>       # 回滚到某个 commit（⚠️ 会丢当前未提交改动）
```

---

## 附 A：零基础操作速查（手动部分全集）

| 动作 | 怎么做 |
|------|-------|
| 打开终端 | Windows: Win+R → 输 `cmd`；Mac: 启动台搜"终端" |
| 进入项目 | `cd 把项目文件夹拖进终端窗口` → 回车 |
| 看当前目录 | `pwd` |
| 列文件 | Windows: `dir` / Mac-Linux: `ls -la` |
| 装依赖 | `npm install` |
| 本地预览 | `npm run dev` → 复制终端里的 `localhost:...` 进浏览器 |
| 提交 | `git add -A` → `git commit -m "描述"` |
| 回滚 | `git reset --hard <哈希>` |

## 附 B：产出清单（本版全部文件）
- ✅ 01-内容结构文档.md（你提供，最新）
- ✅ 02-视觉交互设计系统.md（你提供，最新）
- ✅ 03-技术架构文档.md（本次新增）
- ✅ AGENTS.md（本次重写）
- ✅ PROJECT_STATE.md（本次生成）
- ✅ 完整交接手册.md（本文件）
- ✅ tasks/PHASE1-2 ~ PHASE7（六张，随手册生成）
